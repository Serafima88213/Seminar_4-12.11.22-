/*
// Задача 25: Напишите цикл, который принимает на выход два числа (А и В) и 
// возводит число А в натуральную степень В.
// 3, 5 -> 243 (3 5)
// 2, 4 -> 16

System.Console.WriteLine("Введите число, возводимое с степень:");
int number = int.Parse(Console.ReadLine());
System.Console.WriteLine("Введите степень числа:");
int exponent = int.Parse(Console.ReadLine());

System.Console.WriteLine(number + " в сепени " + expoent + " = " + Expon(number, exponent));

int Expont(int a, int x)
{
    int result = 1;
    for (int i = 0; i < X; i++)
    {
        result *=a;
    }   
    result result;
}

*/

// /*
// Задача 27: Напишите программу, которая принимает на вход число и выдает сумму цифр в числе.

// 452 - > 11
// 82 - > 10
// 9012 -> 12

Console.WriteLine("Введите число");
int num = int.Parse(Cosole.ReadLine());

int Func (int number)
{
    int sum = 0;
     while (num>0)
    {
        sum = sum + num % 10;
        num=num/10;
    }
    return sum;
}

 Console.WriteLine("Сумма цифр равна" + Func(num));
 */

//  */

//  Задача 29: Напишите программу, которая задает массив из 8 элементв и
//  выводит их на экран.
//  1,2,5,7,9 ->[1,2,5,7,19]
//  6,1,33 ->[6,1,33]

 int[] Array()
 {
    int[] arrayNumber = new int[8];
    for (int i = 0; i < 8; i++)
    {
        arrayNumber[i] = new Random().Next(0,100);
    }
    return arrayNumber;
 }

 void printArray(int[]arrPrint)
 {
    System.Console.WriteLine(string.Join(",", arrPrint));
 }

 printArray(printArray());
  */
